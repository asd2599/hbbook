# 로컬 전용 설정 — API 키가 들어갈 수 있어 절대 올리지 않습니다
.claude/settings.local.json

# 사내 원본 자료 — 공개 저장소에 올리지 않습니다 (로컬에만 둡니다)
# CLAUDE.md · docs/slide-guide.md 가 이 경로들을 참조하지만, 저장소에는 포함되지 않습니다
docs/old/
docs/M*자료/
docs/*.csv
docs/*.png
docs/*.pptx
docs/*.docx

# 강의 영상 — 용량이 커서 저장소에 올리지 않습니다
# 교안 HTML 과 **같은 폴더**에 두면 재생됩니다 (배포본은 dist/ 안에 함께 둡니다)
*.mp4

# OS
Thumbs.db
.DS_Store
desktop.ini
