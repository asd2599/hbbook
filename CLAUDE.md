# hdbook — AX 중급과정 HTML 강의 교안

## 프로젝트 목적

**AX 중급과정**(일반직 대상 1일 집합교육)의 강의 교안을 **HTML**로 제작한다.
기존 PDF 교안(`docs/old/`)을 대체하며, 2026-09-04 확정된 신규 커리큘럼을 기준으로 한다.

- 산출물: 모듈별 HTML 교안 (OT, M1~M5, 마무리)
- 대상: 일반직 (비개발 직군) — 개발 지식 없이 따라올 수 있어야 함
- 교육 형태: 08:00~17:00 강사 주도 집합교육 + 실습 20건

## 문서 경로

| 경로 | 설명 |
|---|---|
| `docs/slide-guide.md` | **슬라이드 구현의 단일 기준** — 화면 구조, 스케일 방식, 조작(키보드·휠·화살표), 마크업 규칙, 공통 클래스, 색상, PDF 이미지 추출, 렌더링 확인 절차 |
| `docs/curriculum.md` | **현행 교안 정리본 (기록용)** — 타임테이블, 모듈별 슬라이드 목록, 실습 목록, 준비물, 확인 필요 사항 |
| `docs/old/` | 기존 PDF 교안 6종. **스타일과 화면 이미지만 참고** |
| `docs/M1자료` ~ `docs/M5자료`, `docs/우수사례/` | 화면 캡처·PPT 등 모듈별 원본 자료 (로컬 전용) |

- **무엇을 담을지**는 **교안 HTML 자체가 기준**이다. 고칠 내용은 해당 `*.html`을 직접 읽고 판단하며,
  커리큘럼에 없던 내용을 새로 넣거나 기존 내용을 빼야 하면 먼저 확인을 요청한다.
- `docs/curriculum.md` 는 **교안을 따라 쓰는 기록 문서**다. 기준으로 삼지 않고, 교안을 고친 뒤 함께 갱신한다.
- **어떻게 만들지**는 `docs/slide-guide.md`를 따른다. 교안 HTML을 새로 만들거나 고치기 전에 먼저 읽는다.

## 산출물 구조

```
index.html            목차 — 스크롤 없이 한 화면. M1~M5 카드에서 각 교안으로 진입
m1.html               M1 교안 (완료, 55슬라이드 · 챕터 9)
m2.html               M2 교안 (완료, 33슬라이드 · 챕터 6)
m3.html               M3 교안 (완료, 37슬라이드 · 챕터 5)
m4.html               M4 교안 (완료, 42슬라이드 · 챕터 5)
m5.html               M5 교안 (완료, 20슬라이드 · 챕터 3)
assets/css/deck.css   공통 슬라이드 스타일 (전 모듈 공유)
assets/js/deck.js     슬라이드 이동 · 화면 맞춤 스케일 · 해시 동기화
assets/img/           기존 PDF 교안에서 추출한 화면 캡처 · 로고
assets/실습자료/      교육생 배포용 실습 데이터 (M1 설문 CSV·보고서 양식 · M2 지점실적 CSV · M3 회의메모 · M4 base)
assets/우수사례/      M4 사례 슬라이드가 링크하는 원본 결과물 HTML 5종
tools/build-single.py 배포용 단일 HTML 빌드 스크립트
dist/                 배포본 (빌드 결과물 — 직접 고치지 않는다)
docs/                 커리큘럼 정리본 · 슬라이드 구현 가이드 · 원본 자료
```

### 배포본 만들기

`dist/` 폴더가 그대로 **교육생에게 주는 「강의자료」**다 — 교안 HTML 한 파일 · `assets/우수사례/`(M4 사례 링크 대상)
· `실습자료/`(M2·M3·M4) · 영상 mp4. **폴더째** 전달한다.
M4 교육생은 `실습자료/base` 폴더에서 `code .` → `claude` 로 실습을 시작한다.

교안을 고친 뒤 `python tools/build-single.py` 를 실행하면
목차와 모든 모듈을 합치고 CSS·JS·이미지를 본문에 넣은
**`dist/AX중급과정_교안.html` 한 파일**이 만들어진다 (약 7MB).
받는 사람은 더블클릭만 하면 되고, 인터넷 연결도 설치도 필요 없다.
새 모듈을 추가하면 `tools/build-single.py` 의 `MODULES` 목록에 넣는다.

- 모듈 HTML `<head>` 의 **`<style>` 블록도 함께 실린다.** 모듈 고유 스타일을 `deck.css` 로 옮기지 않는다.
- **영상은 본문에 넣지 않는다.** 교안은 `<video src="파일이름.mp4">` 처럼 **경로 없이 파일 이름만** 쓰고,
  그 mp4 를 **HTML 과 같은 폴더**에 둔다. 배포본이라면 `dist/` 안에 HTML 과 나란히 두면 된다.
  (2026-09-12 **M3 3p 에 실리콘밸리 영상을 되살렸다** — OT 에 있던 그 장이다.
  `실리콘밸리_AI에이전트.mp4` 를 **배포 폴더 안, HTML 과 나란히** 두어야 재생된다.
  원본 `m3.html` 로 미리 볼 때는 **레포 루트에도** 같은 파일이 있어야 한다 —
  용량을 두 번 차지하지 않도록 `dist/` 의 파일에 **하드링크**를 걸어 두었다. mp4 는 `.gitignore` 대상이다.)

### 진행 상태

| 모듈 | 상태 |
|---|---|
| 목차 (`index.html`) | 완료 |
| M1 (`m1.html`) | 완료 — 55슬라이드(챕터 9). **챕터 1 「Agent와 함께 일하기」**(OT 에서 옮겨 옴 — Agent = 알바생 · 사람의 검증 · 알바생 세 명) + **토큰과 대화의 공간**(사용료 · 세션을 아껴 쓰는 법 — M3 에서 이관) + 웹페이지 만들기 + **프로젝트 챕터 3개** — ④ 설문 CSV로 대시보드(CO-STAR 배우고 개선) · ⑤ 같은 메일 답장을 두 번 시켜 비교 · ⑥ 업무보고서 양식(docx)을 리소스로 넣고 **스몰토크로 재료를 모아** 완성본 HTML 제출 — + **커넥터**(M2에서 이관) |
| M2 (`m2.html`) | 완료 — 33슬라이드(챕터 6). **H Chat 데스크탑 전용 모듈** — ① 데스크탑 준비 → ② Agent를 이루는 세 가지(눈·두뇌·손) → ③ 작업 폴더 정하기(파일을 표로 · 날짜별 폴더로 · **권한 허용**) → ④ **폴더 지침으로 실적 합치기**(규칙 한 번 → 통합표 → **규칙 한 줄 바꿔 다시**) → ⑤ 보고서 PDF 만들기(스킬 · PDF) → ⑥ 현업에 쓰기(**문서 보안(DRM) 해제** · **커넥터** · 내 업무로) |
| M3 (`m3.html`) | 완료 — 37슬라이드(챕터 5). **3p 에 실리콘밸리 영상**(약 3분 30초). 배포된 **회의 메모**를 재료로 Claude Code 기초 · CLAUDE.md · **양식 지침 문서** · Skill을 훑는다. **워드는 쓰지 않는다** — 최종 결과물은 `/회의록작성 <메모이름>` 한 줄로 나오는 **회의록 PDF**이고, PDF는 이미 깔려 있는 **엣지를 headless 로 인쇄**해 만든다 (파이썬·npm 설치 없음) |
| M4 (`m4.html`) | 완료 — 42슬라이드(챕터 5). 표지 뒤에 **작품 미리보기 1장 + 우수 사례 5장**(원본 HTML 링크, 챕터 밖)을 두고 "이제 본격적으로 만들어 봅시다"로 넘어간다. 챕터는 **① 작품을 만들기 위한 준비 단계**(App vs AI App · **오늘 만들 것 — 실제 화면 네 장에서 ②에 AI 기능 표시** · **Claude API KEY** — 발급 실습은 없고, 메모장에 없으면 다시 발급받게 안내) → **② 가장 단순한 AI App 만들어보기**(먼저 **강사 시연** — 사외망 기준으로 만든 앱이 사내에서 **연결 실패**하는 화면을 보이고 왜 막히는지·왜 `server.js` 를 주는지 짚은 뒤 → 강의자료의 **`실습자료/base` 폴더에서 `code .` → `claude`** → **“server.js 띄우고 http://localhost:8787 열어줘” 라고 말로 시키고** → **화면에 API 키를 넣어 [연결 테스트]**. **화면도 server.js 가 서빙**하므로 app.html 을 더블클릭하지 않는다 — 포트가 밀려도 어긋나지 않는다) → **③ 멋진 AI App으로 만들기**(PDF 분석 대시보드 · 문서 챗봇) → **④ Agent에게 더 잘 시키는 방법**(Human-in-the-loop) → **⑤ 배포하기**(**검증해 둔 `실행.bat` 을 base 에 넣어 두었다** — 복사해서 zip 만 하면 된다). **npm 설치가 한 번도 필요 없다** — 중계 서버는 Node 기본 모듈만 |
| M5 (`m5.html`) | 완료 — 20슬라이드(챕터 3) (아이디어 기획서란 · 재료 고르기 · Step 1~4 · 내일 첫 행동 · 하루 전체 한 줄 요약). **마무리 정리(16:50~17:00)를 M5 뒤쪽에 포함** |

새 모듈 교안은 `m1.html`을 골격으로 복제해 만든다.
슬라이드 크기·조작·마크업·클래스·색상 등 구현 규칙 전체는
**[`docs/slide-guide.md`](docs/slide-guide.md)** 에 있으므로 작업 전에 그 문서를 읽는다.

핵심만 옮기면:

- 슬라이드는 **1280×720 고정**이고 `deck.js`가 창에 맞춰 통째로 스케일한다.
  **본문이 720px를 넘으면 경고 없이 잘린다.**
- 본문은 세로 가운데 정렬(`justify-content: safe center`)이라 자식 간 **마진이 상쇄되지 않는다.**
- 이동은 키보드(`← →`), 마우스 휠, 상단 화살표, 터치 스와이프 모두 지원한다.
  상단 바의 **번호 칸에 숫자를 넣고 Enter** 하면 그 장으로 바로 간다.
- 교안 안의 **외부 링크는 `deck.js`가 자동으로 새 탭**에서 열도록 처리한다 (HTML에 따로 쓰지 않아도 된다).
- 색상은 기존 PPT에서 추출한 5개 토큰만 쓴다 (`--navy` `#164194` 외). 임의로 색을 추가하지 않는다.
  **예외는 프롬프트 상자 한 곳**이다 — 실제 도구 화면처럼 보이도록 검은 바탕(`--term-bg`) ·
  노란 글씨(`--term-text`) · 이름표 색(`--term-code` 살구 / `--term-chat` 파랑)을 따로 둔다
  (2026-09-12 강사 지시).
- 프롬프트 상자(`.pbox`)에는 **어느 도구에 넣는 프롬프트인지 이름표**가 붙는다.
  기본값은 `<body data-app="...">` 이고, 상자별로 `data-app` 을 달아 바꾼다.
- 슬라이드를 추가하면 상단 바의 `n / N` 과 `index.html` 카드의 "슬라이드 N장"을 함께 갱신한다.
- 모듈은 **챕터**로 나눈다. 챕터마다 네이비 **표지 슬라이드**(`.slide--chapter`)를 한 장 두고,
  그 섹션에 `data-chapter="챕터 이름"` 을 단다. `deck.js` 가 이걸 모아 상단 바 **[챕터] 버튼 →
  왼쪽 서랍** 목차를 만들고, 누르면 그 장으로 간다. 모듈 정리 슬라이드에도 `data-chapter` 를 달아
  서랍 맨 아래에 함께 띄운다. `head-tag` 의 `M1-3 · 개념` 은 **챕터 번호와 일치**시킨다.
- 챕터 서랍은 **기본으로 열려 있고, 슬라이드를 가리지 않는다** — 열리면 `--side-w` 만큼
  `.stage` 가 오른쪽으로 비켜 앉고 스케일도 그만큼 다시 계산된다. 창 너비가 **1180px 미만이면
  접힌 채로** 시작하고, 열려 있다가 좁아져도 접힌다. 여닫기는 [챕터] 버튼과 `Esc` 로만 한다.

## 교안 작성 원칙

1. 일반직 관점의 **업무 문제 → 학습 목적 → 쉬운 개념 설명 → 실습 → 결과 확인 → 현업 적용** 흐름으로 구성
2. 실습은 명칭만 쓰지 않고 **"왜 하는가 / 무엇을 해결하는가 / 무엇이 남는가"** 를 함께 명시
3. 쉬운 실습으로 시작해 난이도를 점진적으로 상승
4. 각 실습 끝에 **현업 적용 예시**와 가능하면 **Before → After** 를 반드시 배치
5. 사내 기밀 대신 **교육용 Mock Data** 사용
6. 외부 Skill 다운로드·GitHub 의존 실습 제외, **사전 제공 자료 중심**
7. 실습 실패 대비 **완료본 / 대체 경로** 명시

### 표현 가이드

- **교안 슬라이드에는 구체적인 시간을 쓰지 않는다.**
  `08:10~09:10`, `60분`, `15분` 같은 시각·소요시간 표기를 넣지 않는다.
  진행 순서가 필요하면 시간 열 없이 **구분(개념/실습/공유)과 내용만** 표로 보여준다.
  시간 정보는 강사·운영진용인 `docs/curriculum.md` 와 목차(`index.html`)에만 둔다.
- 기술 용어보다 업무 비유를 우선한다. **비유는 아래 하나로 통일한다.**
  - **Agent = '아르바이트생 한 명'** — 눈(환경인식) · 두뇌(판단) · 손(도구)으로 이루어진다
  - **모델 = '그 알바생의 IQ(똑똑한 정도)'** — IQ가 높을수록 비싸고 느리다.
    알바생은 그대로 두고 **IQ만 골라 끼운다**. (M1 8p 기준. M2 의 '눈·두뇌·손' 설명에서는
    그 **두뇌가 얼마나 똑똑한지**가 곧 IQ 라고 이어 붙인다)
  - Claude 모델은 **자동차 급**으로 부른다 — Opus 제네시스급(사내 사용 불가) ·
    **Sonnet 그랜저급(오늘 쓰는 모델)** · Haiku 자전거급
  - **Effort = '같은 두뇌로 얼마나 오래 생각할지'**
  - 프로젝트 = '반복업무 전용 작업공간' / 커넥터 = '업무자료와 연결하는 통로' /
    CLAUDE.md = '항상 적용되는 공통 기준' / Skill = '필요할 때 불러 쓰는 반복업무 레시피'
  - ⚠️ **엔진·일꾼 비유는 쓰지 않는다** (2026-09-09에 두뇌·알바생으로 통일함)
- **핵심 비유는 글로만 두지 않고 그림을 함께 붙인다.** 인라인 SVG를 `<div class="figure">` 안에 넣고,
  색은 팔레트 토큰 값만 쓴다. 구현 예: `m2.html` 16p(알바생 전신) · `m1.html` 5p(두뇌 교체).
  비용은 **초록 동그라미 개수**로만 표현하고 실제 금액은 쓰지 않는다 (`₩` 글자는 폰트에 따라 깨진다).
  예외는 **M1 토큰 사용료 한 곳**이다 — 100만 토큰당 입력 `$3` · 출력 `$15`(**입력의 5배**)로 적는다 (`$` 는 깨지지 않는다).
- **명령어 암기를 요구하지 않는다.** 필요한 명령은 교안에 그대로 싣는다.
- Claude Code를 '코딩 도구'가 아닌 **'여러 파일을 읽고 처리하는 업무 Agent'** 로 소개한다.
- 결과물은 코딩이 아니라 **자연어 프롬프트로 만들었다**는 점을 강조한다 (M4 마무리 필수).

## 작업 규칙

- 교안 내용을 임의로 추가·삭제하지 않는다. 지금 교안에 없는 내용이 필요하면 먼저 확인을 요청한다.
- 미확정 사항(점심시간 표기, 사내망 리허설, API 키 발급 경로 등)은 임의 판단하지 말고 확인 후 반영한다.
- 기존 PDF(`docs/old/`)와 모듈별 원본 자료(`docs/M*자료`, `docs/우수사례/`)는 읽기 전용으로 취급하고 수정하지 않는다.
- 교안을 고친 뒤에는 `docs/curriculum.md` 의 해당 부분(슬라이드 목록·장수·실습 목록)도 함께 갱신한다.
- 문서는 UTF-8로 작성한다.
- 슬라이드를 추가·수정한 뒤에는 Playwright로 스크린샷을 찍어 **본문이 잘리지 않는지 눈으로 확인**한다.
